# Runner v1 Codex CLI Smoke

This file is a smoke artifact for Issue #120.

It exists only to prove that `local_runner_v1` can invoke Codex CLI in
review-bundle-only mode and leave an unstaged documentation diff for ChatGPT
review.

This is not new product scope, not a feature milestone, and not a change to the
Local Document-to-Knowledge Workbench product behavior.

## Scope

- Docs-only artifact.
- No source code changes.
- No test changes.
- No runner, workflow, package, or configuration changes.

## Expected Review State

The intended final state is one unstaged change:

```text
docs/RUNNER_V1_CODEX_CLI_SMOKE.md
```
